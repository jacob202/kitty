# Superpowers brainstorm artifacts

This directory holds **brainstorm session output** (HTML previews, layout notes) from the Superpowers plugin. It is not part of Kitty runtime; keep it for design exploration.

Do not commit `state/server.pid` or other ephemeral machine state if you add selective tracking.
