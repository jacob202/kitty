{
  "schema_version": 1,
  "observation_window": {
    "minimum_elapsed_seconds": 3600,
    "minimum_processed_packets": 2
  },
  "tripwires": {
    "maximum_campaign_elapsed_seconds": 14400,
    "maximum_current_packet_elapsed_seconds": 2700,
    "minimum_accepted_packets_per_hour": 1.0,
    "maximum_consecutive_no_substantive_diff": 2,
    "maximum_setup_metadata_fraction": 0.25,
    "maximum_supervisor_to_worker_token_ratio": 1.0,
    "maximum_reset_recovery_events": 3,
    "maximum_repeated_systemic_blocker_count": 2,
    "maximum_projected_vs_simple_baseline_ratio": 2.0
  },
  "required_receipt_fields": [
    "elapsed_seconds",
    "processed_packets",
    "accepted_packets",
    "current_packet_elapsed_seconds",
    "consecutive_no_substantive_diff",
    "setup_metadata_seconds",
    "supervisor_tokens",
    "worker_tokens",
    "reset_recovery_events",
    "repeated_systemic_blocker_count",
    "projected_completion_seconds",
    "simple_baseline_seconds"
  ],
  "decision": {
    "on_tripwire": "pause",
    "on_missing_core_metrics": "insufficient_evidence",
    "on_missing_required_metrics_after_observation_window": "pause",
    "message": "Builder is preserving execution state but is not producing accepted outcomes efficiently. Pause, diagnose the repeated cause, reconsider packet decomposition and model assignment, then resume only with a bounded hypothesis."
  }
}
