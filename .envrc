source venv/bin/activate
# Load project secrets (ANTHROPIC_API_KEY, OPENROUTER_API_KEY, etc.)
# .env is gitignored — copy from .env.example and fill keys before using direnv here.
dotenv_if_exists .env
