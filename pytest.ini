[pytest]
# The default Python suite is hermetic. Only explicitly authorized external-
# provider probes are excluded from normal local and CI runs.
addopts = -m "not controlled_live" --strict-markers
pythonpath = .
testpaths = tests
markers =
    controlled_live: explicitly authorized external-provider contract smoke; never default
