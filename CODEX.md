# Kitty - Codex Guide

Start with `START_HERE.md` and follow its canonical reading order before making changes.

This is a Codex-specific adapter; it does not restate shared doctrine.
Cold-start/receipt branching, engineering and verification rules, reviewer
routing, Builder ownership, Git/authorization limits, and special commands
live in `AGENTS.md`. PR/gate mechanics live in `docs/WORKFLOW.md` and
`docs/reference/PREVENTION_MECHANISMS.md`. The current mission and active
sequence live in `docs/ACTIVE_MISSION.md` and `docs/ROADMAP.md` — read those
for current work rather than any summary here.

## How To Work Here

- Anchor every action in `/Users/jacobbrizinnski/Projects/kitty`.
- Treat `/Users/jacobbrizinski/Documents/Kitty` as stale unless proven otherwise.
- Use `apply_patch` for manual file edits.
- Never print `.env` values or runtime data contents unless Jacob explicitly asks.
