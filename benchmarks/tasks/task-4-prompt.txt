Extract the `log_error` function from `src/core/error_handler.py` into a new file `src/core/error_logger.py`. The `log_error` function logs errors to a JSONL file. Its dependencies are: `json`, `Path` (from pathlib), `classify_error` (from error_handler.py), and `datetime`. After extracting, update all imports across the project so the code still works. Do NOT duplicate the `log_error` function — it should exist only in the new file.
