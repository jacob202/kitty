# LiteLLM — Kitty model surface
#
# The kitty-* names are ROLES, not models. App code asks for a role; this file
# decides which provider answers. Check what's live with:
#
#   curl -s localhost:8000/api/model-routing | jq
#
# or open Settings → providers in the UI. Both show alias → provider → model
# and whether the key is actually set.
#
#   kitty-default  → primary brain
#   kitty-sonnet   → deep tier
#   kitty-small    → cheap/fast
#   kitty-vision   → vision tasks, image gen
#
# To switch providers: change litellm_params.model below (litellm's
# "<provider>/<model>" form), point api_key at the matching env var, then
# restart with `bash gateway/start_litellm.sh`. There is no in-app switch.
#
# WARNING: every route below is OpenRouter, and kitty-default falls back to
# kitty-small which is also OpenRouter. One dead balance takes out all of Kitty.
# A second provider on kitty-small would make the fallback chain mean something.
#
# Run: bash gateway/start_litellm.sh
#
# Env: OPENROUTER_API_KEY, LITELLM_MASTER_KEY

model_list:
  - model_name: kitty-default
    litellm_params:
      model: openrouter/deepseek/deepseek-v4-pro
      api_key: os.environ/OPENROUTER_API_KEY
      max_tokens: 16384

  - model_name: kitty-sonnet
    litellm_params:
      model: openrouter/deepseek/deepseek-v4-pro
      api_key: os.environ/OPENROUTER_API_KEY
      max_tokens: 16384

  - model_name: kitty-small
    litellm_params:
      model: openrouter/deepseek/deepseek-v4-flash
      api_key: os.environ/OPENROUTER_API_KEY
      max_tokens: 4096

  - model_name: kitty-vision
    litellm_params:
      model: openrouter/mistral/mistral-small-latest
      api_key: os.environ/OPENROUTER_API_KEY
      max_tokens: 4096

router_settings:
  routing_strategy: usage-based-routing-v2
  num_retries: 1
  timeout: 120

litellm_settings:
  drop_params: true
  set_verbose: false
  num_retries: 1
  fallbacks:
    - kitty-default: [kitty-small]

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
