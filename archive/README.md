# Archive

Trees under `archive/` are **historical or superseded** code and docs. They are **not** part of the live Kitty product path.

**Do not** import from here in new work. The runnable stack is under `gateway/`, launched via `kitty_gateway/` and `./kitty`.

For the canonical stack, ports, and verification commands, see **`docs/ARCHITECTURE.md`**.
