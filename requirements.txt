fastapi==0.140.7
uvicorn==0.51.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
filelock==3.32.0
httpx==0.28.1
requests==2.34.2
aiohttp==3.14.3
websockets==16.1.1
python-socketio==5.16.3
feedparser==6.0.14
edge-tts==7.2.8
faster-whisper==1.2.1
chromadb==1.5.8
# Upper bound is mem0ai 0.1.x's own openai ceiling (line 30). Raising this
# without moving mem0ai first makes the tree unresolvable and reds main.
openai>=1.90.0,<1.110.0
anthropic==0.120.2
langgraph==1.2.2
numpy==2.5.1
pillow==11.3.0

# Gmail connector (P3, docs/packets/005) — read-only OAuth + REST.
google-auth==2.56.2
google-auth-oauthlib==1.4.0

# retry + memory + article extraction (for brief enrichment)
tenacity>=8.5.0,<10
mem0ai>=0.1.118,<0.2
# gateway/memory.py configures mem0's Ollama embedder. mem0 treats this client
# as optional and prompts on stdin when it is missing, which under launchd is an
# EOFError that takes all of Kitty's memory down. Declare it so it is never absent.
ollama>=0.4,<1
trafilatura>=1.12,<3
