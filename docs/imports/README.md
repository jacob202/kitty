# Imported External Notes

Use this folder for exported chats, notebook text, and other external research before mining it into active Kitty docs.

## Gemini Kitty UI

Expected file:

```text
docs/imports/gemini-kitty-ui.md
```

Paste or export the full Gemini conversation there when the signed-in browser can access it. Then review it for:

- UI code worth keeping
- architectural decisions
- rejected ideas
- bug reports or verified fixes
- text that should move into active docs before archive cleanup

Do not treat a blocked share URL as harvested content. If the imported file is missing, the Gemini source is still unreviewed.
