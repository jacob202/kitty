<!--
  Grep tag: LEARNINGS-72H
  Purpose: Rolling digest of multi-agent / multi-session work over ~72 hours.
  Canonical coordination rows: docs/AGENT_COORDINATION.md → Learnings Log (L-00x).
-->

# 72-Hour Learnings Digest — 2026-04-28 → 2026-04-30 (Kitty)

**Window**: approximately seventy-two hours ending **2026-04-30** (America/Regina).  
**Repo**: legacy checkout `/Users/jacobbrizinski/Projects/kitty` (canonical git).  
**Runtime copy**: `/Users/jacobbrizinski/Projects/kitty-system/kitty-app` (rsync target, no `.git`).

---

## 1. Executive summary

The tree moved quickly: **coordination protocol**, **audits**, **Phase A blockers**, **Phase B/C/D surface work**, **API refactors** (`internal_api_only`, streaming DRY), **operational-plan closure** for Phase A, and **merge-gate / copy-first** discipline. Tests stayed **green at ~393** through the period. Main risks were **process** (commit message vs staged files, merge-gate report path), **documentation drift** (audit tables vs later reality), and **environment** (`Icon\r`, API keys in CI logs). No show-stopping production defect was identified in review; several **hardening opportunities** remain below.

---

## 2. Timeline (high level)

| Time (local) | Theme |
|----------------|--------|
| Apr 30 early | Domain news, skills, merge-gate reports, TASKS updates |
| Apr 30 ~19:00–21:00 | Web default mode, stream regression test, Phase B/C doc + Garage UI polish |
| Apr 30 ~20:41 | Phase A code blockers (MemoryWeave path, SOUL, honcho unregister, shims, KittyCoder, route guards) |
| Apr 30 ~21:05–23:20 | Phase D capability (OBD watcher, MLX model, voice, KB), internal API gate, streaming helpers, `/chat` DRY |
| Apr 30 ~23:27+ | Coordination closeout, merge gate re-run, Phase A **documented** complete in operational plan |
| Apr 30 ~23:36 | Builder automation: generate specs from intake MD (`d579639`) |

---

## 3. What went well

- **Test gate**: Pre-commit + full `pytest tests/` caught regressions before merge; suite scale (~393) is a strong safety net.
- **Copy-first separation**: `scripts/copy_workspace_separation.py --execute` gives a repeatable promoted runtime without touching canonical git history.
- **API hygiene**: Centralizing `internal_api_only` and DRYing supervisor broadcast paths reduced copy-paste failure modes.
- **Milestone traceability**: `specs/`, `docs/audits/operational-plan-20260430.md`, and lane handoffs created a navigable story for parallel agents.

---

## 4. Errors, near-misses, and inconsistencies

| Severity | Item | Detail |
|----------|------|--------|
| Medium | **Merge gate report** | First run produced a report **missing the header and Full Suite section**. Re-run with an **absolute `--report` path** fixed it. Root cause class: **relative paths + invocation context**; treat as operational hazard until script resolves paths. |
| Low | **Commit vs message** | Example: a commit message referenced `AGENT_COORDINATION` while the commit primarily carried `TASKS`, merge-gate doc, and `PARKED_FEATURES` — **staged set did not match narrative**. |
| Low | **Stale audit rows** | `operational-plan-20260430.md` **Audit Summary** still lists pre-fix findings (e.g. KittyCoder stub) while **Phase A** below is marked complete — readers can misread unless they scroll. |
| Low | **401 noise in tests** | Provider fallback logs **401** during pytest; suite passes but **noise hides real failures** if a future test ignores stderr. |
| Info | **`honcho_routes.py`** | Lived on as an empty blueprint file after registration removal; later **deleted** in cleanup — orphan files confuse audits. |
| Info | **Dual “Phase” narratives** | `CURRENT_FOCUS`, coordination lanes, and `HANDOFF.md` can describe different phases simultaneously — needs periodic **reconciliation** (not necessarily more docs, one source of “what’s next”). |

---

## 5. Opportunities (next leverage)

1. **`run_phase4_merge_gate.sh`**: Resolve `--report` to an absolute path after args parse (or `realpath`) so reports are always well-formed; keep **absolute path** in runbooks until then.
2. **Operational plan**: Add a one-line **“Audit summary frozen at …”** note or refresh summary metrics after major phases (optional; avoids contradiction).
3. **`news_bp`**: Exported in `src/api/__init__.py` but **not registered in `web.py`** — either register intentionally or document omission (diff vs old “migrated” audits).
4. **Streaming `/chat`**: Further unify timeout/cancel pattern with `spawn_broadcasting_task` only if semantics stay identical (avoid behavior drift).
5. **DECISIONS promotion**: Several learnings below merit eventual **D-00xx** if they recur (merge gate path, dual-workspace protocol).

---

## 6. Recommendations (actionable)

| # | Recommendation | Owner |
|---|----------------|-------|
| R1 | Before every commit: `git diff --cached --stat` and align the **first line of the message** with that stat. | All agents |
| R2 | After every meaningful legacy commit: **run copy script** before someone tests on `kitty-app`. | All agents |
| R3 | Merge gate: **server up** on smoke port + **absolute report path** + archive reports under `docs/archive/…` when cleaning root `docs/`. | CI / humans |
| R4 | When renaming helpers (`_run_with_app_context` → `run_with_app_context`), **rg the old symbol** across `src/` before declaring done. | Implementers |
| R5 | Keep **one “next action”** truth: `TASKS.md` **Next smallest action** should stay consistent with `CURRENT_FOCUS` **Current task** within 24h of phase shifts. | Head / user |
| R6 | For provider keys in tests: prefer **monkeypatched client** or **explicit env unset** in those tests to avoid **401 log spam** and flaky rate limits. | Test maintainers |

---

## 7. Feedback (quality / process)

- **Strength**: Multi-lane coordination (`AGENT_COORDINATION.md`) scaled better than ad-hoc chat handoffs.
- **Watch**: “Phase complete” claims in handoffs should point at **verifiable artifacts** (commit SHA, test command, report path) — several handoffs already do; keep that bar.
- **Watch**: Large same-day commit volume — bisect-friendly **smaller commits** reduce rollback pain even when tests are green.

---

## 8. Mapping to coordination Learnings Log

New rows appended in `docs/AGENT_COORDINATION.md` (IDs **L-005–L-011**). This digest is the **narrative expansion**; the table is the **index**.

---

## 9. Verification commands (standing)

```bash
cd /Users/jacobbrizinski/Projects/kitty && venv/bin/python -m pytest tests/ -q --tb=short
/opt/homebrew/bin/python3.12 scripts/copy_workspace_separation.py --project /Users/jacobbrizinski/Projects/kitty --execute
# With server on PORT:
bash scripts/run_phase4_merge_gate.sh --project /Users/jacobbrizinski/Projects/kitty-system/kitty-app --port 5001 --report "$(pwd)/docs/PHASE4_MERGE_GATE_RUN_$(date +%F)_manual.md"
```

---

*Digest authored: cursor agent, 2026-04-30. Tag for search: **LEARNINGS-72H**.*
