# Workspace separation move map (pointer)

Last updated: 2026-05-02

The **detailed move map** for the physical workspace separation experiment lives in the archive:

`docs/archive/2026-04-29-audits-and-logs/WORKSPACE_SEPARATION_MOVE_MAP.md`

The copy-first `kitty-system/kitty-app` workspace was **reconciled back** into `/Users/jacobbrizinski/Projects/kitty` and removed (see `docs/audits/CONSOLIDATION_REPORT_2026-05-01.md`). Treat this pointer as governance glue only; do not execute moves from the archived map without a new approved spec.
