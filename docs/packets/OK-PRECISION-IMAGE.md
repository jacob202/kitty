# OK-PRECISION-IMAGE — Image Lab Precision Migration

## Mission

Apply the shared visual precision system to the settled Image Lab creative workspace without altering provider routing, generation truth, or the active Image implementation lane.

## Hard hold

Do not start while `feat/wow-image-studio-20260831` or its successor has uncommitted/actively owned Image files. Re-run collision check first.

## Depends on

- settled Image Studio integration
- `OK-PRECISION-01`

## Scope

- References -> Create -> Results -> Activity hierarchy
- prompt/composer geometry
- candidate grid spacing
- inspector/action alignment
- estimate/cost/status typography
- lineage presentation
- mobile stacking

## Acceptance

- generated results visually dominate provider mechanics;
- selected references/characters are clear but not noisy;
- prompt and generate controls align with shared control system;
- cost/status stays visible without overwhelming creative task;
- candidate grid is stable/responsive;
- long metadata does not cause horizontal scroll;
- provider/debug details remain behind disclosure unless they require attention.

## Non-goals

- provider changes;
- recipe semantics;
- identity pipeline changes;
- generation bug fixes except regressions introduced by this migration.
