{
  "manifest_version": 1,
  "initiative_id": "ktf-003-outcome6-runtime-v1",
  "title": "KTF-003: Outcome 6 runtime — continue unrelated work and pause honestly on provider exhaustion",
  "description": "Phase 1 Outcome 6 cannot be proven on current main. ADR 0021 requires an exhausted packet not to stop unrelated eligible packets, but gateway/builder_run.py currently pauses and returns after every non-success. Free-provider exhaustion is also reported as an ordinary implementation failure instead of a durable resumable availability pause. These two sequential free-exec packets make the runtime match the already-ratified policy before the daylight proof.",
  "packets": [
    {
      "id": "KTF-FE-04-continue-unrelated-after-failure",
      "title": "Continue unrelated eligible packets after exhaustion",
      "objective": "Execution class: free-exec. One outcome only: after a packet exhausts or is cancelled, `run_initiative` must continue to the next unrelated eligible packet instead of pausing the whole initiative. Dependent packets remain blocked by the existing exhausted-packet eligibility rules.\n\nIn `gateway/builder_run.py`, replace this exact block:\n\n        if loop_result[\"outcome\"] != \"succeeded\":\n            assert classification is not None  # set in the exhaustion branch above\n            pause_reason = f\"packet {packet_id} exhausted\"\n            if classification[\"stop_class\"] == STOP_NEEDS_DECISION:\n                pause_reason += f\" [needs_decision: {classification['reason']}]\"\n            else:\n                pause_reason += f\": {loop_result.get('reason')}\"\n            bi.pause_initiative(\n                initiative_id,\n                pause_reason,\n                db_path=db_path,\n            )\n            return {\n                \"outcome\": \"paused\",\n                \"reason\": f\"packet {packet_id} exhausted\",\n                \"stop_class\": classification[\"stop_class\"],\n                \"processed\": processed,\n                \"succeeded\": succeeded,\n                \"exhausted\": exhausted,\n            }\n\nwith this exact block:\n\n        if loop_result[\"outcome\"] != \"succeeded\":\n            assert classification is not None  # set in the exhaustion branch above\n            _decide(\n                task_id,\n                {\n                    \"initiative_id\": initiative_id,\n                    \"packet_id\": packet_id,\n                    \"decision\": \"continued_after_packet_failure\",\n                    \"reason\": loop_result.get(\"reason\"),\n                    \"stop_class\": classification[\"stop_class\"],\n                    \"stop_class_reason\": classification[\"reason\"],\n                },\n                db_path,\n            )\n            # The exhausted/cancelled packet is no longer eligible. Loop again so\n            # Builder can select unrelated approved work; dependencies on this\n            # packet remain blocked by builder_initiative's existing rules.\n            continue\n\nIn `tests/test_builder_run.py`, make these three exact assertion replacements:\n\n1. In `test_scope_escalation_run_needs_decision_with_findings`, replace:\n        assert summary[\"outcome\"] == \"paused\"\nwith:\n        assert summary[\"outcome\"] == \"idle\"\n        assert summary[\"exhausted\"] == 1\n\n2. In `test_three_different_failures_is_routine`, replace:\n        assert summary[\"outcome\"] == \"paused\"\n        assert summary[\"stop_class\"] == br.STOP_ROUTINE\n        status = bi.initiative_status(INITIATIVE, db_path=db_path)\n        assert status[\"stop_class\"] == br.STOP_ROUTINE\nwith:\n        assert summary[\"outcome\"] == \"idle\"\n        assert summary[\"exhausted\"] == 1\n        status = bi.initiative_status(INITIATIVE, db_path=db_path)\n        assert status[\"state\"] == bi.INITIATIVE_FAILED\n        assert status[\"stop_class\"] == br.STOP_ROUTINE\n\n3. In `test_same_signature_exhaustion_needs_decision_ambiguous`, replace:\n        assert summary[\"outcome\"] == \"paused\"\n        assert summary[\"stop_class\"] == br.STOP_NEEDS_DECISION\n\n        status = bi.initiative_status(INITIATIVE, db_path=db_path)\nwith:\n        assert summary[\"outcome\"] == \"idle\"\n        assert summary[\"exhausted\"] == 1\n\n        status = bi.initiative_status(INITIATIVE, db_path=db_path)\n        assert status[\"state\"] == bi.INITIATIVE_FAILED\n        assert status[\"stop_class\"] == br.STOP_NEEDS_DECISION\n\nThen append this test verbatim at the end of class `TestStopClassIntegration`, immediately before `class TestCp06AutoMerge`:\n\n    def test_exhausted_packet_does_not_stop_unrelated_packet(\n        self, repo: Path, db_path: Path, tmp_path: Path\n    ):\n        failing = _packet(\"P1\")\n        failing[\"acceptance_criteria\"] = [\"impossible.txt exists\"]\n        failing[\"validation_commands\"] = [\"test -f impossible.txt\"]\n        succeeding = _packet(\"P2\")\n        _apply(db_path, [failing, succeeding], repo_root=repo)\n\n        worker = tmp_path / \"selective.sh\"\n        worker.write_text(\n            \"#!/bin/sh\\nset -e\\n\"\n            \"packet_id=$(python3 -c \"\n            \"\\\"import json; print(json.load(open('$KB_BUNDLE_PATH'))['packet_id'])\\\")\\n\"\n            \"if [ \\\"$packet_id\\\" = \\\"P2\\\" ]; then echo ok > done.txt; fi\\n\"\n            f\"printf '%s\\\\n' '{_GOOD_IMPL}' > \\\"$KB_RESULT_PATH\\\"\\n\",\n            encoding=\"utf-8\",\n        )\n        worker.chmod(0o755)\n\n        summary = br.run_initiative(\n            INITIATIVE,\n            worker_command=[\"/bin/sh\", str(worker)],\n            db_path=db_path,\n            repo_root=repo,\n        )\n\n        assert summary[\"outcome\"] == \"idle\", summary\n        assert summary[\"exhausted\"] == 1\n        assert summary[\"succeeded\"] == 1\n        assert [item[\"packet_id\"] for item in summary[\"processed\"]] == [\"P1\", \"P2\"]\n        status = bi.initiative_status(INITIATIVE, db_path=db_path)\n        assert status[\"state\"] == bi.INITIATIVE_FAILED\n        assert status[\"exhausted\"] == [\"P1\"]\n        assert status[\"done\"] == [\"P2\"]\n\nChange nothing else. Stopping rule: if any quoted block or named test anchor is absent or occurs more than once, make no changes and fail the attempt.",
      "depends_on": [],
      "acceptance_criteria": [
        "An exhausted packet does not pause the initiative while another unrelated packet is eligible.",
        "The unrelated packet runs in the same `run_initiative` invocation and can succeed.",
        "The exhausted packet and its dependents remain ineligible under the existing initiative rules.",
        "Needs-decision and routine stop classifications remain visible in durable decision evidence without globally stopping unrelated work."
      ],
      "allowed_paths": [
        "gateway/builder_run.py",
        "tests/test_builder_run.py"
      ],
      "policy": {
        "max_attempts": 2,
        "priority": 10
      },
      "validation_commands": [
        "python3.12 -m pytest tests/test_builder_run.py -q --tb=short",
        "python3.12 -m ruff check gateway/builder_run.py tests/test_builder_run.py",
        "grep -q 'continued_after_packet_failure' gateway/builder_run.py && grep -q 'test_exhausted_packet_does_not_stop_unrelated_packet' tests/test_builder_run.py"
      ]
    },
    {
      "id": "KTF-FE-05-provider-exhaustion-pause",
      "title": "Treat free-provider exhaustion as a resumable availability pause",
      "objective": "Execution class: free-exec. One outcome only: when every configured free worker or reviewer provider fails cleanly, Builder records provider exhaustion as an availability condition, returns the packet to a resumable queued state, pauses the initiative durably, and does not consume the packet's implementation attempt budget.\n\nMake the following exact edits.\n\n1. In `scripts/kittybuilder_opencode_worker.sh`, inside the final `if [[ -z \"${chosen_model}\" ]]` block, replace:\n  exit 1\nwith:\n  exit 75\nChange only that exit in that block.\n\n2. In `scripts/kittybuilder_opencode_reviewer.sh`, inside the final `if [[ -z \"${chosen_model}\" ]]` block, replace:\n  exit 1\nwith:\n  exit 75\nChange only that exit in that block.\n\n3. In `gateway/builder_loop.py`, immediately after:\nLOOP_CANCELLED = \"cancelled\"\nadd:\n\nLOOP_PROVIDER_EXHAUSTED = \"provider_exhausted\"\nPROVIDER_EXHAUSTED_EXIT_CODE = 75\n\nImmediately after the existing `_record_infrastructure_failure` function, add this helper verbatim:\n\ndef _close_provider_exhaustion(\n    *,\n    initiative_id: str,\n    packet_id: str,\n    task_id: str,\n    attempt: dict[str, Any],\n    lease: dict[str, Any],\n    entry: dict[str, Any],\n    history: list[dict[str, Any]],\n    manifest: dict[str, Any],\n    manifest_path: Path,\n    reason: str,\n    phase: str,\n    db_path: Path | None,\n) -> dict[str, Any]:\n    \"\"\"Close a clean provider outage without charging the implementation budget.\"\"\"\n    entry[\"outcome\"] = ba.ATTEMPT_CRASHED\n    entry[\"failure\"] = reason\n    entry[\"provider_exhausted\"] = True\n    manifest[\"outcome\"] = LOOP_PROVIDER_EXHAUSTED\n    manifest[\"failure\"] = _text_evidence(reason)\n    write_run_manifest(manifest_path, manifest)\n    _close_bound_attempt(attempt, lease, ba.ATTEMPT_CRASHED, db_path=db_path)\n    _record_infrastructure_failure(\n        task_id,\n        reason=reason,\n        phase=phase,\n        attempt_id=attempt[\"id\"],\n        db_path=db_path,\n    )\n    task = bq.get_task(task_id, db_path=db_path)\n    if task is not None and task[\"state\"] == bq.BLOCKED:\n        bq.operator_release_task(\n            task_id,\n            reason=\"provider_exhausted_resumable_pause\",\n            db_path=db_path,\n        )\n    final_task = bq.get_task(task_id, db_path=db_path)\n    return {\n        \"outcome\": LOOP_PROVIDER_EXHAUSTED,\n        \"initiative_id\": initiative_id,\n        \"packet_id\": packet_id,\n        \"task_id\": task_id,\n        \"task_state\": final_task[\"state\"] if final_task else None,\n        \"reason\": reason,\n        \"attempts\": history,\n    }\n\nImmediately after the existing assignment:\n        write_run_manifest(manifest_path, manifest)\n\nthat follows construction of `manifest[\"worker_run\"]`, insert:\n\n        if (\n            run.get(\"exit_code\") == PROVIDER_EXHAUSTED_EXIT_CODE\n            and not run_report.get(\"changed_paths\")\n        ):\n            return _close_provider_exhaustion(\n                initiative_id=initiative_id,\n                packet_id=packet_id,\n                task_id=task_id,\n                attempt=attempt,\n                lease=lease,\n                entry=entry,\n                history=history,\n                manifest=manifest,\n                manifest_path=manifest_path,\n                reason=\"all configured free worker providers were unavailable\",\n                phase=\"worker_provider_exhaustion\",\n                db_path=db_path,\n            )\n\nInside the review section, immediately after:\n            if review_error is not None:\n                failure = review_error\nreplace those two lines with:\n\n            if (\n                review_error is not None\n                and review_error.startswith(\n                    f\"review command exited {PROVIDER_EXHAUSTED_EXIT_CODE}:\"\n                )\n            ):\n                return _close_provider_exhaustion(\n                    initiative_id=initiative_id,\n                    packet_id=packet_id,\n                    task_id=task_id,\n                    attempt=attempt,\n                    lease=lease,\n                    entry=entry,\n                    history=history,\n                    manifest=manifest,\n                    manifest_path=manifest_path,\n                    reason=\"all configured free reviewer providers were unavailable\",\n                    phase=\"review_provider_exhaustion\",\n                    db_path=db_path,\n                )\n            if review_error is not None:\n                failure = review_error\n\n4. In `gateway/builder_run.py`, immediately after the complete `if loop_result[\"outcome\"] == \"succeeded\": ...` branch and before its current `else:` exhaustion branch, change that `else:` to this exact branch sequence:\n\n        elif loop_result[\"outcome\"] == bl.LOOP_PROVIDER_EXHAUSTED:\n            processed.append(\n                {\n                    \"packet_id\": packet_id,\n                    \"task_id\": task_id,\n                    \"outcome\": loop_result[\"outcome\"],\n                }\n            )\n            _decide(\n                task_id,\n                {\n                    \"initiative_id\": initiative_id,\n                    \"packet_id\": packet_id,\n                    \"decision\": \"provider_exhausted\",\n                    \"reason\": loop_result.get(\"reason\"),\n                    \"stop_class\": STOP_ROUTINE,\n                },\n                db_path,\n            )\n            bi.pause_initiative(\n                initiative_id,\n                f\"provider exhaustion on {packet_id}; resumable when providers recover\",\n                db_path=db_path,\n            )\n            return {\n                \"outcome\": \"paused\",\n                \"reason\": f\"provider exhaustion on {packet_id}\",\n                \"stop_class\": STOP_ROUTINE,\n                \"processed\": processed,\n                \"succeeded\": succeeded,\n                \"exhausted\": exhausted,\n            }\n        else:\n\nDo not duplicate the normal `processed.append` later in the function: the provider branch returns before it.\n\n5. In `tests/test_builder_run.py`, append these tests verbatim at the end of class `TestStopClassIntegration`, immediately before `class TestCp06AutoMerge`:\n\n    def test_worker_provider_exhaustion_pauses_without_consuming_attempt_budget(\n        self, repo: Path, db_path: Path, tmp_path: Path\n    ):\n        _apply(db_path, [_packet(\"P1\"), _packet(\"P2\")], repo_root=repo)\n        unavailable = tmp_path / \"provider-unavailable.sh\"\n        unavailable.write_text(\"#!/bin/sh\\nexit 75\\n\", encoding=\"utf-8\")\n        unavailable.chmod(0o755)\n\n        summary = br.run_initiative(\n            INITIATIVE,\n            worker_command=[\"/bin/sh\", str(unavailable)],\n            db_path=db_path,\n            repo_root=repo,\n        )\n\n        assert summary[\"outcome\"] == \"paused\"\n        assert \"provider exhaustion\" in summary[\"reason\"]\n        assert summary[\"processed\"][0][\"outcome\"] == br.bl.LOOP_PROVIDER_EXHAUSTED\n        attempts = br.ba.list_attempts(INITIATIVE, \"P1\", db_path=db_path)\n        assert [attempt[\"outcome\"] for attempt in attempts] == [br.ba.ATTEMPT_CRASHED]\n        status = bi.initiative_status(INITIATIVE, db_path=db_path)\n        assert status[\"state\"] == bi.INITIATIVE_PAUSED\n        assert status[\"eligible\"] == [\"P1\", \"P2\"]\n\n    def test_reviewer_provider_exhaustion_is_resumable(\n        self, repo: Path, db_path: Path, tmp_path: Path\n    ):\n        _apply(db_path, [_packet(\"P1\")], repo_root=repo)\n        unavailable_review = tmp_path / \"review-provider-unavailable.sh\"\n        unavailable_review.write_text(\"#!/bin/sh\\nexit 75\\n\", encoding=\"utf-8\")\n        unavailable_review.chmod(0o755)\n\n        committing_worker = tmp_path / \"committing-worker.sh\"\n        committing_worker.write_text(\n            \"#!/bin/sh\\nset -e\\n\"\n            \"echo ok > done.txt\\n\"\n            \"git add done.txt\\n\"\n            \"git -c user.email=t@t -c user.name=t commit -q -m \\\"[P1] implementation\\\"\\n\"\n            f\"printf '%s\\\\n' '{_GOOD_IMPL}' > \\\"$KB_RESULT_PATH\\\"\\n\",\n            encoding=\"utf-8\",\n        )\n        committing_worker.chmod(0o755)\n\n        summary = br.run_initiative(\n            INITIATIVE,\n            worker_command=[\"/bin/sh\", str(committing_worker)],\n            review_command=[\"/bin/sh\", str(unavailable_review)],\n            db_path=db_path,\n            repo_root=repo,\n        )\n\n        assert summary[\"outcome\"] == \"paused\"\n        assert \"provider exhaustion\" in summary[\"reason\"]\n        attempts = br.ba.list_attempts(INITIATIVE, \"P1\", db_path=db_path)\n        assert [attempt[\"outcome\"] for attempt in attempts] == [br.ba.ATTEMPT_CRASHED]\n        status = bi.initiative_status(INITIATIVE, db_path=db_path)\n        assert status[\"state\"] == bi.INITIATIVE_PAUSED\n        assert status[\"eligible\"] == [\"P1\"]\n\nChange nothing else. Stopping rule: if any named anchor is absent or ambiguous, or if either adapter's final all-provider-failed block cannot be identified uniquely, make no changes and fail the attempt. Do not alter any other non-zero exits.",
      "depends_on": [
        "KTF-FE-04-continue-unrelated-after-failure"
      ],
      "acceptance_criteria": [
        "A clean all-provider failure from the free worker or reviewer returns a distinct provider-exhausted packet outcome.",
        "Provider exhaustion closes the open attempt as crashed so it does not consume the implementation attempt budget.",
        "The packet task is returned to queued, the initiative is paused with a durable provider-exhaustion reason, and resuming can select the packet again.",
        "Ordinary implementation, validation, scope, identity, and reviewer failures retain their existing classifications.",
        "Only the adapters' final all-provider-failed exits use code 75."
      ],
      "allowed_paths": [
        "scripts/kittybuilder_opencode_worker.sh",
        "scripts/kittybuilder_opencode_reviewer.sh",
        "gateway/builder_loop.py",
        "gateway/builder_run.py",
        "tests/test_builder_run.py"
      ],
      "policy": {
        "max_attempts": 2,
        "priority": 9
      },
      "validation_commands": [
        "python3.12 -m pytest tests/test_builder_run.py tests/test_kittybuilder_opencode_adapters.py -q --tb=short",
        "python3.12 -m ruff check gateway/builder_loop.py gateway/builder_run.py tests/test_builder_run.py",
        "grep -q 'LOOP_PROVIDER_EXHAUSTED = \"provider_exhausted\"' gateway/builder_loop.py && grep -q 'provider_exhausted_resumable_pause' gateway/builder_loop.py && grep -q 'test_worker_provider_exhaustion_pauses_without_consuming_attempt_budget' tests/test_builder_run.py && grep -q 'test_reviewer_provider_exhaustion_is_resumable' tests/test_builder_run.py",
        "test \"$(grep -c 'exit 75' scripts/kittybuilder_opencode_worker.sh)\" -eq 1 && test \"$(grep -c 'exit 75' scripts/kittybuilder_opencode_reviewer.sh)\" -eq 1"
      ]
    }
  ]
}
