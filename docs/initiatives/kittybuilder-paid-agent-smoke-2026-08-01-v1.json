{
  "manifest_version": 1,
  "initiative_id": "kittybuilder-paid-agent-smoke-2026-08-01-v1",
  "title": "Disposable paid smoke evidence: real-agent delivery proof 2026-08-01",
  "description": "Paid-leg evidence task accepted by the operator audit of 2026-08-01, run on the paid default route (deepseek-v4-flash-0731). The sole outcome is one research file under docs/research/ recording the delivery facts. It must never touch production application code or claim success beyond the evidence it records.",
  "packets": [
    {
      "id": "PAID-01-create-paid-evidence-file",
      "title": "Create the paid real-agent smoke evidence file",
      "objective": "Execution class: paid-exec, single attempt, paid default model. One outcome only: create the file docs/research/kittybuilder-paid-agent-smoke-2026-08-01.md in this worktree, whose content is exactly the sections below, and nothing else beyond minimal markdown headers and a final empty line.\n\nStart by running `git rev-parse HEAD` in the current worktree; the printed 40-character SHA is the exact main SHA this task started from. Write it verbatim as the Starting SHA value.\n\nRead the packet context bundle to find the task and attempt identity: glob for a file named `.kittybuilder-bundle-*.json` in the worktree root (the context manifest file is named `.kittybuilder-context-*.json`; its `attempt_id` field is the numeric attempt id, and its `bundle_sha256` must match the `sha256` in the `context.task_bundle` object). Record:\n- Task ID: the bundle's `task_id` field\n- Attempt number: the bundle's `attempt_no` field\n- Attempt ID: the context manifest's `attempt_id` field (a positive integer)\n- Worker ID: paid-openrouter\n\nRecord the model and provider as configured for this dispatch: Model = openrouter/deepseek/deepseek-v4-flash-0731, Provider = openrouter. These are the exact values of the paid default route for this attempt; do not invent a different model or provider.\n\nWrite the file with these exact field labels and values:\n\n# KittyBuilder paid smoke evidence (2026-08-01)\n\nStarting main SHA: <40-char SHA from the first git rev-parse HEAD above>\nProvider: openrouter\nModel: openrouter/deepseek/deepseek-v4-flash-0731\nInitiative ID: kittybuilder-paid-agent-smoke-2026-08-01-v1\nPacket ID: PAID-01-create-paid-evidence-file\nTask ID: <bundle task_id>\nAttempt ID: <context manifest attempt_id>\nAttempt number: <bundle attempt_no>\nWorker ID: paid-openrouter\n\n## What KittyBuilder did\n\nWrite exactly five non-empty lines under this heading, each a single factual sentence describing this delivery only:\n1. An operator dispatched this packet to KittyBuilder on the paid default route with a one-attempt budget.\n2. KittyBuilder created an isolated git worktree for the task from the recorded starting main SHA and ran the worker inside it.\n3. The paid worker authored this file inside that worktree, within the packet's allowed path docs/research/.\n4. The worker left the commit to the runner, which committed the file on the task branch with the packet marker in the commit subject.\n5. This file is disposable smoke evidence for the delivery-path audit and makes no claim about merging or production behavior.\n\n## Hard rule: never run git commit yourself\n\nDo NOT run git commit, git add with a commit, git commit --amend, git reset, or any command that writes commit history. The runner owns the commit and will commit your change with a required [PAID-01-create-paid-evidence-file] marker in the subject. A self-commit without the marker fails identity verification even if the file content is perfect, so leave the worktree dirty (file written, not committed) and let the runner commit.\n\nThe field lines above and all sentences in the five-line section must be truthful and precise. Do not add invented facts: no PR number, no merge claim, no claim that validation or review passed (they run after the worker finishes), no claim about other machines. Do not edit any file except docs/research/kittybuilder-paid-agent-smoke-2026-08-01.md. Do not create directories beyond what exists. Run the declared validation commands to check your file, then write the result JSON. Stopping rule: if any of the required source values (rev-parse HEAD output, bundle task_id, attempt_no, context attempt_id) cannot be read, make no changes and fail the attempt.",
      "depends_on": [],
      "acceptance_criteria": [
        "docs/research/kittybuilder-paid-agent-smoke-2026-08-01.md exists on the task branch only.",
        "The recorded starting SHA matches git rev-parse HEAD^ (the task branch's parent commit, which is the durable main base the worktree was created from).",
        "All required fields are present with correct labels: provider openrouter, model openrouter/deepseek/deepseek-v4-flash-0731, initiative id, packet id, task id, numeric attempt id and attempt number, worker id paid-openrouter.",
        "What KittyBuilder did contains exactly five non-empty lines and no invented success claims.",
        "The runner's commit subject carries the [PAID-01-create-paid-evidence-file] marker and the deterministic validation command exits 0 on the committed tree."
      ],
      "allowed_paths": [
        "docs/research/"
      ],
      "policy": {
        "max_attempts": 1,
        "priority": 10
      },
      "validation_commands": [
        "F=docs/research/kittybuilder-paid-agent-smoke-2026-08-01.md; test -f \"$F\" && grep -qF \"$(git rev-parse HEAD^)\" \"$F\" && grep -qF \"Provider: openrouter\" \"$F\" && grep -qF \"Model: openrouter/deepseek/deepseek-v4-flash-0731\" \"$F\" && grep -qF \"Initiative ID: kittybuilder-paid-agent-smoke-2026-08-01-v1\" \"$F\" && grep -qF \"Packet ID: PAID-01-create-paid-evidence-file\" \"$F\" && grep -qE \"^Attempt ID: [0-9]+$\" \"$F\" && grep -qE \"^Attempt number: 1$\" \"$F\" && grep -qF \"Worker ID: paid-openrouter\" \"$F\" && awk '/^## What KittyBuilder did/{f=1;next} f&&/^## /{exit} f&&NF{c++} END{exit !(c==5)}' \"$F\"",
        "git diff --check",
        "git log -1 --format=%s | grep -qF '[PAID-01-create-paid-evidence-file]'"
      ]
    }
  ]
}
