# Codebase Map → moved

The authoritative codebase map is now at [`docs/reference/CODEBASE_MAP.md`](reference/CODEBASE_MAP.md).

This file is retained as a redirect. See the new location for current repository navigation, Kitty/KittyBuilder boundary, directory tree, entry points, data flows, state ownership, testing layers, CI overview, and common change locations.
