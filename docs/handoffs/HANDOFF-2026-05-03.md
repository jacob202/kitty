# Phase 0 Close-out — 2026-05-03

**Shipped:**
- Two atomic commits: `6274bd6` (merge_gate marker + docs), `9afa84c` (--interactive flag CLI fix)
- venv recreated from clean state (Icon\r contamination gone)
- 3 contract tests: empty args → exit 2, project-only → exit 2, spec-only → exit 2
- conftest.py Icon\r sanity check: aborts pytest with exit 3 if Finder artifacts detected
- Pre-commit hook: refuses commits containing .DS_Store, ._\*, Icon\*
- Memory-growth reproduction: pre-fix commit confirmed REPL hang (process stays alive indefinitely on `input()`). Single process ~18 MB stable — the "memory skyrocket" was likely multiple hung pytest subprocesses, not a single-process leak.

**Verification:** 430 passed, 5 deselected, ~24s

**Residual risks:** mlx_lm import chain takes ~10-15s (slow but not broken); the import-time delay makes subprocess contract tests slower than ideal.

**Next:** Phase 1 — thorough cleanup (stale docs, sibling workbench, dead code, caches, corrupt memory data).
