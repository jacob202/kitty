# Phase 1.1 — Builder recovery proof

**Verdict:** PASS
**Initiative:** `phase1-1-recovery-proof-20260801-214311`
**HEAD:** `bcae5f28fcb5a11573faeea29862231a9335b7fa`
**Ran:** 2026-08-01T21:43:11Z → 2026-08-01T21:43:42Z

Generated by `scripts/builder_recovery_proof.py`. Every scenario runs
against the real Builder queue with deterministic local worker
fixtures — no model provider is contacted and no spend occurs.
Reproduce with:

```bash
python3.12 scripts/builder_recovery_proof.py --json
```

## Scenarios

| Scenario | Verdict | Checks |
| --- | --- | --- |
| Worker crash mid-execution | PASS | 4/4 |
| Stale claim lease reconciliation | PASS | 5/5 |
| Out-of-scope debris is caught and preserved | PASS | 4/4 |
| Interrupted review stays honest | PASS | 4/4 |
| Provider exhaustion is distinct | PASS | 4/4 |
| Worker failure, operator grant, operator completion, independent review | PASS | 9/9 |
| Clean completion after the failures | PASS | 2/2 |
| Doctor consistency across the run | PASS | 1/1 |

### Worker crash mid-execution (`RP-01-crash-proto-20260801-214311`)

- **PASS** — the crashed attempt is not recorded as succeeded
  - observed: `"failed"`
- **PASS** — the real kill signal is recorded as the exit code
  - observed: `-9`
- **PASS** — the failure is attributed to the worker, not invented
  - observed: `["worker_failed"]`
- **PASS** — partial work written before the crash survives in the worktree
  - observed: `true`

Evidence:

```json
{
  "run_outcome": "exhausted",
  "attempt_outcome": "failed",
  "exit_code": -9,
  "blocked_reasons": [
    "worker_failed"
  ],
  "partial_output_path": "/home/user/kitty/.worktrees/kittybuilder/kb_msaweaho_e092/data/recovery_proof/rp01.txt"
}
```

### Stale claim lease reconciliation (`RP-02-stale-lease-proto-20260801-214311`)

- **PASS** — the task was genuinely claimed before the lease expired
  - observed: `"claimed"`
- **PASS** — the recovery scan requeued the expired claim
  - observed: `1`
- **PASS** — the task is queued again rather than left claimed
  - observed: `"queued"`
- **PASS** — the release is written to the event trail, not applied silently
  - observed: `["claimed", "created", "released"]`
- **PASS** — the packet completes normally after recovery
  - observed: `"succeeded"`

Evidence:

```json
{
  "state_while_claimed": "claimed",
  "recover_summary": {
    "claimed_requeued": 1,
    "running_blocked": 0,
    "total": 1,
    "runs_interrupted": 0,
    "run_ids": [],
    "starting_runs_deferred": 0,
    "starting_run_ids": [],
    "runs_unverified": 0,
    "unverified_runs": [],
    "running_tasks_blocked": 0,
    "claimed_tasks_requeued": 0,
    "conflicts": 0
  },
  "state_after_recover": "queued",
  "rerun_outcome": "succeeded"
}
```

### Out-of-scope debris is caught and preserved (`RP-03-dirty-worktree-proto-20260801-214311`)

- **PASS** — the attempt is recorded as failed, not succeeded
  - observed: `"failed"`
- **PASS** — the failure names the out-of-scope change specifically
  - observed: `"worker changed files outside the packet's allowed scope"`
- **PASS** — the uncommitted debris survives the failed attempt
  - observed: `true`
- **PASS** — the dirty worktree is reported rather than hidden
  - observed: `{"dirty_files": [" M AGENTS.md"], "changed_paths": ["AGENTS.md"]}`

Evidence:

```json
{
  "attempt_outcome": "failed",
  "failure": "worker changed files outside the packet's allowed scope",
  "dirty_files": [
    " M AGENTS.md"
  ],
  "changed_paths": [
    "AGENTS.md"
  ],
  "debris_path": "/home/user/kitty/.worktrees/kittybuilder/kb_msaweahp_4282/AGENTS.md"
}
```

### Interrupted review stays honest (`RP-04-interrupted-review-proto-20260801-214311`)

- **PASS** — the implementation is recorded as completed
  - observed: `"completed"`
- **PASS** — validation is recorded as passed
  - observed: `"passed"`
- **PASS** — the attempt is not reported as succeeded without a review
  - observed: `"failed"`
- **PASS** — the failure blames the review interruption, not the worker
  - observed: `"review command exited -9: "`

Evidence:

```json
{
  "implementation_status": "completed",
  "validation_status": "passed",
  "attempt_outcome": "failed",
  "failure": "review command exited -9: "
}
```

### Provider exhaustion is distinct (`RP-05-provider-exhausted-proto-20260801-214311`)

- **PASS** — the run reports provider exhaustion, not a worker failure
  - observed: `"provider_exhausted"`
- **PASS** — the attempt is flagged as provider-exhausted
  - observed: `true`
- **PASS** — the task returns to queued so the packet stays resumable
  - observed: `"queued"`
- **PASS** — the exhausted attempt is budget-neutral
  - observed: `false`

Evidence:

```json
{
  "run_outcome": "provider_exhausted",
  "task_state": "queued",
  "attempt_outcome": "crashed",
  "provider_exhausted": true,
  "packet_exhausted": false,
  "packet_state": "queued",
  "attempts_used": 1,
  "attempt_budget": 2
}
```

### Worker failure, operator grant, operator completion, independent review (`RP-07-operator-closeout-proto-20260801-214311`)

- **PASS** — the worker failure is recorded, not overwritten by the operator
  - observed: `true`
- **PASS** — the exhausted budget refuses a further attempt until granted
  - observed: `{"exit": 1, "stderr": "error: phase1-1-recovery-proof-20260801-214311/RP-07-operator-closeout-proto-20260801-214311 has used 1/1 attempts; operator intervention required"}`
- **PASS** — the public grant-attempt command records exactly one further attempt
  - observed: `{"granted": 1, "previous_effective_limit": 1, "new_effective_limit": 2, "reason": "recovery proof: operator grant after worker exhaustion", "event_id": 70}`
- **PASS** — a new attempt starts after the grant
  - observed: `7`
- **PASS** — the operator completion is recorded as an operator action
  - observed: `{"type": "report_attached", "payload": {"report": {"outcome": "completed", "summary": "operator completed the packet by hand after the worker failed", "completion_sha": "bcae5f28fcb5a11573faeea29862231a9335b7fa"}, "operator": true, "reason": "recovery proof: operator-completed closeout"}}`
- **PASS** — the independent review approval is recorded
  - observed: `"approve"`
- **PASS** — all three facts render together, with no fabricated worker success
  - observed: `[true, true, true]`
- **PASS** — the review was accepted onto the operator's attempt
  - observed: `0`
- **PASS** — the worker attempt stays failed and only the operator's succeeds
  - observed: `["failed", "succeeded"]`

Evidence:

```json
{
  "worker_attempt_id": 6,
  "operator_attempt_id": 7,
  "worker_failed_after_failure": true,
  "start_attempt_refused_exit": 1,
  "start_attempt_refused_stderr": "error: phase1-1-recovery-proof-20260801-214311/RP-07-operator-closeout-proto-20260801-214311 has used 1/1 attempts; operator intervention required",
  "grant": {
    "granted": 1,
    "previous_effective_limit": 1,
    "new_effective_limit": 2,
    "reason": "recovery proof: operator grant after worker exhaustion",
    "event_id": 70
  },
  "record_review_exit": 0,
  "final": {
    "worker_failed": true,
    "operator_completed": true,
    "review_approved": true,
    "review_verdict": "approve",
    "operator_action": {
      "type": "report_attached",
      "payload": {
        "report": {
          "outcome": "completed",
          "summary": "operator completed the packet by hand after the worker failed",
          "completion_sha": "bcae5f28fcb5a11573faeea29862231a9335b7fa"
        },
        "operator": true,
        "reason": "recovery proof: operator-completed closeout"
      }
    },
    "attempts_used": 2,
    "attempt_outcomes": [
      "failed",
      "succeeded"
    ],
    "done": false
  }
}
```

### Clean completion after the failures (`RP-06-clean-completion-proto-20260801-214311`)

- **PASS** — the packet succeeds after five induced failures
  - observed: `"succeeded"`
- **PASS** — implementation and validation are both recorded green
  - observed: `["completed", "passed"]`

Evidence:

```json
{
  "run_outcome": "succeeded",
  "implementation_status": "completed",
  "validation_status": "passed"
}
```

### Doctor consistency across the run (`doctor`)

- **PASS** — inducing five failures adds no new doctor FAIL
  - observed: `{"before_fail": 0, "after_fail": 0}`

Evidence:

```json
{
  "before": {
    "pass": 13,
    "warn": 1,
    "fail": 0
  },
  "after": {
    "pass": 13,
    "warn": 1,
    "fail": 0
  }
}
```

## Doctor consistency

- baseline: `{"pass": 13, "warn": 1, "fail": 0}`
- final: `{"pass": 13, "warn": 1, "fail": 0}`
