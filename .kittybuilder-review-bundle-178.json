{
  "bundle_version": 1,
  "initiative_id": "PUBLIC-GOLDEN-PATH-001",
  "packet_id": "PGP-001-CI-PARITY",
  "task_id": "kb_msudvnes_9eaa",
  "attempt_no": 3,
  "objective": "Make local publication gates catch the same required Vulture and Lychee blockers as .github/workflows/tests.yml. Root cause already verified: GitHub runs Vulture and Lychee while scripts/hooks/pre-push and make ci omit both. Also resolve the already-reproduced 404 in docs/archive/fable-context/COLLECTION-BUNDLE.md without globally suppressing GitHub links. Non-goals: no deployment/auth/runtime changes, no broad docs cleanup, no advisory deptry/pip-audit/bandit promotion, no unrelated refactor.",
  "acceptance_criteria": [
    "tests/test_pre_push_gate.py first contains regression coverage proving the hook and Makefile include the required Vulture and Lychee gates represented by .github/workflows/tests.yml.",
    "scripts/hooks/pre-push runs required Vulture and Lychee checks on every push and fails closed when either returns nonzero; it does not claim CI parity while omitting them.",
    "Makefile exposes the same required hygiene checks through make ci, while existing lint/typecheck/pytest/frontend behavior remains intact.",
    "The historical svd-ai-lab/open-engineer 404 no longer causes Lychee failure and no broad github.com ignore is added.",
    "Durable evidence includes a Builder attempt with exact implementation SHA, changed paths, validation output, and review verdict; rollback is a single revert because this packet adds no persistent state migration."
  ],
  "allowed_paths": [
    "tests/test_pre_push_gate.py",
    "scripts/hooks/pre-push",
    "Makefile",
    "docs/archive/fable-context/COLLECTION-BUNDLE.md"
  ],
  "policy": {
    "max_attempts": 3,
    "priority": 200
  },
  "validation_commands": [
    "python -m pytest -q tests/test_pre_push_gate.py",
    "python -m vulture gateway/ --min-confidence 80 --exclude gateway/kitty-chat/",
    "lychee --root-dir docs --no-progress --accept 200,301,302,307,308 docs/",
    "git diff --check origin/main...HEAD"
  ],
  "prior_attempts": [
    {
      "attempt_no": 1,
      "outcome": "failed",
      "implementation": null,
      "review": null
    },
    {
      "attempt_no": 2,
      "outcome": "failed",
      "implementation": null,
      "review": null
    }
  ]
}
